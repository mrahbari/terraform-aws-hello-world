## In Terraform, the terraform block is used to configure global settings and define the required version of Terraform itself.
## The configuration states that the minimum required version of Terraform is 0.12 or any higher version.
terraform {
  required_version = ">= 0.12"
}
